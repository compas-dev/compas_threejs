// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.5
// source: compas_pb/data/datastructures.proto
/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PointData } from "./geometry";
export const protobufPackage = "compas_pb.data";
function createBaseFaceList() {
    return { indices: [] };
}
export const FaceList = {
    encode(message, writer = new BinaryWriter()) {
        writer.uint32(10).fork();
        for (const v of message.indices) {
            writer.uint32(v);
        }
        writer.join();
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFaceList();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag === 8) {
                        message.indices.push(reader.uint32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.indices.push(reader.uint32());
                        }
                        continue;
                    }
                    break;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            indices: globalThis.Array.isArray(object?.indices)
                ? object.indices.map((e) => globalThis.Number(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.indices?.length) {
            obj.indices = message.indices.map((e) => Math.round(e));
        }
        return obj;
    },
    create(base) {
        return FaceList.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFaceList();
        message.indices = object.indices?.map((e) => e) || [];
        return message;
    },
};
function createBaseMeshData() {
    return { guid: undefined, name: undefined, vertices: [], faces: [] };
}
export const MeshData = {
    encode(message, writer = new BinaryWriter()) {
        if (message.guid !== undefined) {
            writer.uint32(10).string(message.guid);
        }
        if (message.name !== undefined) {
            writer.uint32(18).string(message.name);
        }
        for (const v of message.vertices) {
            PointData.encode(v, writer.uint32(26).fork()).join();
        }
        for (const v of message.faces) {
            FaceList.encode(v, writer.uint32(34).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMeshData();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.guid = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.vertices.push(PointData.decode(reader, reader.uint32()));
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.faces.push(FaceList.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            guid: isSet(object.guid) ? globalThis.String(object.guid) : undefined,
            name: isSet(object.name) ? globalThis.String(object.name) : undefined,
            vertices: globalThis.Array.isArray(object?.vertices)
                ? object.vertices.map((e) => PointData.fromJSON(e))
                : [],
            faces: globalThis.Array.isArray(object?.faces)
                ? object.faces.map((e) => FaceList.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.guid !== undefined) {
            obj.guid = message.guid;
        }
        if (message.name !== undefined) {
            obj.name = message.name;
        }
        if (message.vertices?.length) {
            obj.vertices = message.vertices.map((e) => PointData.toJSON(e));
        }
        if (message.faces?.length) {
            obj.faces = message.faces.map((e) => FaceList.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return MeshData.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseMeshData();
        message.guid = object.guid ?? undefined;
        message.name = object.name ?? undefined;
        message.vertices =
            object.vertices?.map((e) => PointData.fromPartial(e)) || [];
        message.faces = object.faces?.map((e) => FaceList.fromPartial(e)) || [];
        return message;
    },
};
function createBaseFaceData() {
    return { vertexIndices: [] };
}
export const FaceData = {
    encode(message, writer = new BinaryWriter()) {
        writer.uint32(10).fork();
        for (const v of message.vertexIndices) {
            writer.int32(v);
        }
        writer.join();
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFaceData();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag === 8) {
                        message.vertexIndices.push(reader.int32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.vertexIndices.push(reader.int32());
                        }
                        continue;
                    }
                    break;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            vertexIndices: globalThis.Array.isArray(object?.vertexIndices)
                ? object.vertexIndices.map((e) => globalThis.Number(e))
                : globalThis.Array.isArray(object?.vertex_indices)
                    ? object.vertex_indices.map((e) => globalThis.Number(e))
                    : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.vertexIndices?.length) {
            obj.vertexIndices = message.vertexIndices.map((e) => Math.round(e));
        }
        return obj;
    },
    create(base) {
        return FaceData.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFaceData();
        message.vertexIndices = object.vertexIndices?.map((e) => e) || [];
        return message;
    },
};
function createBasePolyhedronData() {
    return { guid: undefined, name: undefined, vertices: [], faces: [] };
}
export const PolyhedronData = {
    encode(message, writer = new BinaryWriter()) {
        if (message.guid !== undefined) {
            writer.uint32(10).string(message.guid);
        }
        if (message.name !== undefined) {
            writer.uint32(18).string(message.name);
        }
        for (const v of message.vertices) {
            PointData.encode(v, writer.uint32(26).fork()).join();
        }
        for (const v of message.faces) {
            FaceData.encode(v, writer.uint32(34).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePolyhedronData();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.guid = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.vertices.push(PointData.decode(reader, reader.uint32()));
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.faces.push(FaceData.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            guid: isSet(object.guid) ? globalThis.String(object.guid) : undefined,
            name: isSet(object.name) ? globalThis.String(object.name) : undefined,
            vertices: globalThis.Array.isArray(object?.vertices)
                ? object.vertices.map((e) => PointData.fromJSON(e))
                : [],
            faces: globalThis.Array.isArray(object?.faces)
                ? object.faces.map((e) => FaceData.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.guid !== undefined) {
            obj.guid = message.guid;
        }
        if (message.name !== undefined) {
            obj.name = message.name;
        }
        if (message.vertices?.length) {
            obj.vertices = message.vertices.map((e) => PointData.toJSON(e));
        }
        if (message.faces?.length) {
            obj.faces = message.faces.map((e) => FaceData.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return PolyhedronData.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBasePolyhedronData();
        message.guid = object.guid ?? undefined;
        message.name = object.name ?? undefined;
        message.vertices =
            object.vertices?.map((e) => PointData.fromPartial(e)) || [];
        message.faces = object.faces?.map((e) => FaceData.fromPartial(e)) || [];
        return message;
    },
};
function isSet(value) {
    return value !== null && value !== undefined;
}
