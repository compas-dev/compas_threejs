// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               v6.33.5
// source: compas_pb/data/message.proto
/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../google/protobuf/any";
import { Value } from "../../google/protobuf/struct";
export const protobufPackage = "compas_pb.data";
function createBaseAnyData() {
    return { message: undefined, value: undefined, fallback: undefined };
}
export const AnyData = {
    encode(message, writer = new BinaryWriter()) {
        if (message.message !== undefined) {
            Any.encode(message.message, writer.uint32(10).fork()).join();
        }
        if (message.value !== undefined) {
            Value.encode(Value.wrap(message.value), writer.uint32(18).fork()).join();
        }
        if (message.fallback !== undefined) {
            FallbackData.encode(message.fallback, writer.uint32(26).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseAnyData();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.message = Any.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.value = Value.unwrap(Value.decode(reader, reader.uint32()));
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.fallback = FallbackData.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            message: isSet(object.message) ? Any.fromJSON(object.message) : undefined,
            value: isSet(object?.value) ? object.value : undefined,
            fallback: isSet(object.fallback)
                ? FallbackData.fromJSON(object.fallback)
                : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.message !== undefined) {
            obj.message = Any.toJSON(message.message);
        }
        if (message.value !== undefined) {
            obj.value = message.value;
        }
        if (message.fallback !== undefined) {
            obj.fallback = FallbackData.toJSON(message.fallback);
        }
        return obj;
    },
    create(base) {
        return AnyData.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseAnyData();
        message.message =
            object.message !== undefined && object.message !== null
                ? Any.fromPartial(object.message)
                : undefined;
        message.value = object.value ?? undefined;
        message.fallback =
            object.fallback !== undefined && object.fallback !== null
                ? FallbackData.fromPartial(object.fallback)
                : undefined;
        return message;
    },
};
function createBaseFallbackData() {
    return { data: undefined };
}
export const FallbackData = {
    encode(message, writer = new BinaryWriter()) {
        if (message.data !== undefined) {
            DictData.encode(message.data, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFallbackData();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.data = DictData.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            data: isSet(object.data) ? DictData.fromJSON(object.data) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.data !== undefined) {
            obj.data = DictData.toJSON(message.data);
        }
        return obj;
    },
    create(base) {
        return FallbackData.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFallbackData();
        message.data =
            object.data !== undefined && object.data !== null
                ? DictData.fromPartial(object.data)
                : undefined;
        return message;
    },
};
function createBaseListData() {
    return { items: [] };
}
export const ListData = {
    encode(message, writer = new BinaryWriter()) {
        for (const v of message.items) {
            AnyData.encode(v, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListData();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.items.push(AnyData.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            items: globalThis.Array.isArray(object?.items)
                ? object.items.map((e) => AnyData.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.items?.length) {
            obj.items = message.items.map((e) => AnyData.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return ListData.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListData();
        message.items = object.items?.map((e) => AnyData.fromPartial(e)) || [];
        return message;
    },
};
function createBaseDictData() {
    return { items: {} };
}
export const DictData = {
    encode(message, writer = new BinaryWriter()) {
        globalThis.Object.entries(message.items).forEach(([key, value]) => {
            DictData_ItemsEntry.encode({ key: key, value }, writer.uint32(10).fork()).join();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDictData();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    const entry1 = DictData_ItemsEntry.decode(reader, reader.uint32());
                    if (entry1.value !== undefined) {
                        message.items[entry1.key] = entry1.value;
                    }
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            items: isObject(object.items)
                ? globalThis.Object.entries(object.items).reduce((acc, [key, value]) => {
                    acc[key] = AnyData.fromJSON(value);
                    return acc;
                }, {})
                : {},
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.items) {
            const entries = globalThis.Object.entries(message.items);
            if (entries.length > 0) {
                obj.items = {};
                entries.forEach(([k, v]) => {
                    obj.items[k] = AnyData.toJSON(v);
                });
            }
        }
        return obj;
    },
    create(base) {
        return DictData.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseDictData();
        message.items = globalThis.Object.entries(object.items ?? {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = AnyData.fromPartial(value);
            }
            return acc;
        }, {});
        return message;
    },
};
function createBaseDictData_ItemsEntry() {
    return { key: "", value: undefined };
}
export const DictData_ItemsEntry = {
    encode(message, writer = new BinaryWriter()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== undefined) {
            AnyData.encode(message.value, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDictData_ItemsEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.value = AnyData.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? AnyData.fromJSON(object.value) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== undefined) {
            obj.value = AnyData.toJSON(message.value);
        }
        return obj;
    },
    create(base) {
        return DictData_ItemsEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseDictData_ItemsEntry();
        message.key = object.key ?? "";
        message.value =
            object.value !== undefined && object.value !== null
                ? AnyData.fromPartial(object.value)
                : undefined;
        return message;
    },
};
function createBaseMessageData() {
    return { data: undefined, version: undefined };
}
export const MessageData = {
    encode(message, writer = new BinaryWriter()) {
        if (message.data !== undefined) {
            AnyData.encode(message.data, writer.uint32(10).fork()).join();
        }
        if (message.version !== undefined) {
            writer.uint32(18).string(message.version);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMessageData();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.data = AnyData.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.version = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            data: isSet(object.data) ? AnyData.fromJSON(object.data) : undefined,
            version: isSet(object.version)
                ? globalThis.String(object.version)
                : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.data !== undefined) {
            obj.data = AnyData.toJSON(message.data);
        }
        if (message.version !== undefined) {
            obj.version = message.version;
        }
        return obj;
    },
    create(base) {
        return MessageData.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseMessageData();
        message.data =
            object.data !== undefined && object.data !== null
                ? AnyData.fromPartial(object.data)
                : undefined;
        message.version = object.version ?? undefined;
        return message;
    },
};
function isObject(value) {
    return typeof value === "object" && value !== null;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
