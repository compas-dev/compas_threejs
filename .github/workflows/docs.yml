name: docs

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: compas-dev/compas-actions/docs@v1
        with:
          python-version: "3.11"
          extras: dev
          strict: false
